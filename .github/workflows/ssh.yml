on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  run_pull:
    name: run pull
    runs-on: ubuntu-latest

    steps:
    - name: connect and pull
      run: ssh ${{ secrets.USERNAME }}:${{ secrets.PASSWORD }}@${{ secrets.HOST }} "ls"
